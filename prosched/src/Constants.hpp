#pragma once

namespace prosched {
    constexpr int MAX_PROCESSES = 10;            // limit for scheduler-test
    constexpr int NUM_PRINT_INSTRUCTIONS = 100;  // default prints per process
    constexpr int TICK_DURATION_MS = 10;         // duration of a single tick in milliseconds
}